- dm blast? Probably just a server @ is fine
- handle edge cases for registration (e.g. wrong popflash link for a given discord ID)
- What happens if someone has a popflash account but hasnt played a popflash before?? IE missing data